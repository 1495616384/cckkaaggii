*正在请求专家建议，优化解决方案，该过程耗时可能较长，请耐心等待...*

复核完成。第 14 轮的 Simulation Theorem 确如你所指出，是一个对所有表示都成立的平凡 no-go——我接受，并撤回它作为新颖性证据。本轮回到原始问题，只做结构判定。
The user is correcting me (again) — they point out that round 14's Simulation Theorem trivialized the problem by allowing the baseline to copy the candidate representation. That's a valid criticism: my Theorem S was a trivial no-go that applies to ANY representation, so it proves nothing specific about Address. I need to acknowledge that.

Now they want:
1. Explain where round 14 swapped the research question
2. Strict definition of Address-native computation
3. Distinguish transport / coordinate-local / intrinsic (P1/P2/P3)
4. Re-audit depth-Address (which standard object is it: filtered algebra / pro-algebra / etc.)
5. Find at least 3 classes of non-truncation Address
6. For each, say whether it's still just a standard mathematical object
7. Status: proved / unproved / candidate
8. Answer I/II/III/IV

And crucially: don't make speed the criterion. The question is whether Address can be a structured, composable, decode-free computation space.

Let me think about the math carefully.

**Part 1: Where round 14 swapped the question**

Round 14's Theorem S: the baseline BIT(S) is allowed to store any derived data, including the candidate representation R(x) itself. So Cost_BIT ≤ Cost_R trivially for every R. This is a no-go for the entire notion of "representation novelty via complexity", not a proof about Address specifically. So:
- It proves: "Address cannot beat a baseline that is allowed to copy Address"
- It does NOT prove: "Address has no intrinsic computational structure"

This is a genuine logical point. The right framing: the question is not comparative (beat all indexes) but intrinsic (does ⊙ use A's structure, and is that structure non-trivial relative to the transported one).

**Part 2: Strict definition of Address-native computation**

R: T → A. ⊙: A×A→A with R(f(x,y)) = R(x) ⊙ R(y).

Key point: this equation alone is satisfied by ⊙ = R∘f∘R^{-1} (transport). So the equation is NOT the content. The content must be about *how ⊙ is defined*.

Cases:
- Case 1 (transport): ⊙ = R∘f∘R^{-1}. Pure re-presentation.
- Case 2 (coordinatewise/local): A has coordinates (a_i)_i and ⊙ computed by a finite local rule from the coordinates, e.g., (a_i)·(b_i) — but careful: coordinatewise only works if the coordinate map is a homomorphism, i.e., π_i(R(f(x,y))) = F_i(π_i(Rx), π_i(Ry)). So coordinatewise requires the π_i to be homomorphisms. And then ⊙ = the induced map on the product of coordinates. But is that "new"? If A = ∏_i A_i and R = (π_i ∘ R)_i, then R is an embedding into the product, and ⊙ is the product of the F_i. This is exactly the "subdirect/product representation" situation. The A is a subalgebra of ∏ A_i.
- Case 3 (intrinsic): A has operations/projections such that ⊙ is defined by A's own structural rules and is not the coordinatewise expansion of f.

Crucial mathematical insight: if π_i ∘ R is a homomorphism for each i, and the π_i jointly separate points of A (or of im R), then the map R: T → ∏_i A_i is a homomorphism. So Case 2 IS Case 1 relative to the product target: A ≅ subalgebra of ∏ A_i, and ⊙ is transported f restricted to the image. Hmm — so Case 2 collapses to Case 1 at the level of the pair (A, ⊙)?

Let me think. Let A be the carrier, and suppose A comes with projections π_i: A → A_i and each π_i ∘ R: T → A_i is a homomorphism, i.e., π_i(R(f(x,y))) = F_i(π_i(Rx), π_i(Ry)). Now define A' = the subalgebra of ∏_i A_i generated by (π_i(R(x)))_i. Then R' : T → A' is a surjective homomorphism (if the π_i separate points of im R). And A' is a homomorphic image of T. So (A', ⊙') ≅ T/ker R' — a quotient of T.

So: Case 2 (coordinatewise with homomorphic coordinates) = a quotient of T presented as a subdirect product. That's still "transport" in the sense that ⊙' is determined by f modulo the congruence.

But wait — there's a subtlety the user is pointing at. The question isn't whether A is isomorphic to a quotient of T (that's trivially true for any surjective homomorphism). The question is whether **A has structure beyond (carrier, ⊙)** — i.e., whether A carries operations/projections/relations that are part of its definition and that ⊙ uses.

This is exactly what rounds 6-8 explored: enriched structure = (A, F, Π, {F_i}). And the conclusion there was: it's a 2-sorted algebra with coherence; the "is it a renaming" question = whether ker π has a congruence complement (Theorem P).

So: the honest answer is that at the *algebraic* level, the enrichment question was settled in rounds 6-8. What's new here is a *different* question: what does "the representation participates in computation" mean in terms of the *definition* of ⊙?

Let me try to make this precise. Define:

**Definition (realization independence).** Let R: T → A, and let ⊙ realize R along f. Call R **T-native** if ⊙ is the only operation on A making R a homomorphism. Hmm, that's about uniqueness.

Better framing: The user wants a distinction between "⊙ is defined by referencing T" and "⊙ is defined by a rule internal to A". This is a *syntactic/definitional* distinction, which is hard to make into a theorem... but let's try.

**Key idea: define via a "definitional closure" notion.** Given a structure A with primitive operations and projections Π = {π_i}, let Def(A, Π) = the set of operations on A definable (parameter-free FO, or term) from {(primitive ops), (π_i)}. Then:

- P1 (transported): ⊙ ∈ Def(A, Π) but *only* via a definition that mentions R or T. Hmm, R is not part of A's signature.

Actually, the cleanest formulation: **the question is whether ⊙ ∈ Def(A, Π)** where Def is definability within the structure (A, primitives, projections) — no reference to T. Because if ⊙ is definable from A's own structure, then ⊙ is "intrinsic to A". If ⊙ requires knowing R (i.e., requires decoding), it's transported.

And this connects to rounds 12-13's "decodability" idea! Recall: P = "F is definable in the structure (A, {π_i})" — round 13 (my answer) exactly had this. Let me use it.

**Three levels:**
- **P1 (transported)**: ∃R, ⊙ with R(f(x,y)) = R(x) ⊙ R(y). Always satisfiable (take A = T, R = id, ⊙ = f, or any R).
- **P2 (coordinate-local)**: A carries projections {π_i} with π_i ∘ R a homomorphism for each i, and ⊙ is computable coordinatewise from finitely many π_i's. Then ⊙ ∈ Def(A, Π) trivially? Hmm.
- **P3 (intrinsic)**: ⊙ ∈ Def(A, Π) but ⊙ is NOT the unique/only such operation... 

Hmm, let me think about what genuinely distinguishes P2 from P3. The user's phrasing: "Address 内部存在新的组合结构，使得 ⊙ 可以直接由 Address 的有限结构规则构造，且这种结构不是简单把原 tree operation 在坐标上逐层展开."

So P2 = coordinatewise expansion of f (like the depth-truncation case: F_n transported f). P3 = ⊙ defined by a structural rule of A that is *not* just the coordinatewise expansion.

Mathematically, if ⊙ is definable from (A, Π) and A's structure is determined by the π_i's, then ⊙ is essentially forced by the coordinate data. Let's formalize:

**Proposition (P2 ⟹ P1 under separation).** Let π_i: A → A_i (i ∈ I) be projections with π_i ∘ R homomorphisms, and suppose {π_i} jointly separates points of A. Then R: T → ∏_i A_i is a homomorphism, and A ≅ im(R) as algebras, with ⊙ = the restriction. So (A, ⊙) ≅ T/ker(R) — a quotient of T presented subdirectly.

So P2 does not give a new algebra; it gives a subdirect product presentation (which is standard). The "newness" of P2 over P1 is presentational (the subdirect presentation), not algebraic.

Then what could P3 be? P3 requires A to have structure that is NOT captured by the π_i's being homomorphisms. I.e., A has primitive operations or relations that are NOT determined by the coordinate data, and ⊙ uses them.

That's exactly the "enrichment" case from rounds 6-8: A = (carrier, F, Π, {F_i}) where the Π are intrinsic. And round 8's Theorem P settled: is it a product (P1) or not? Answer: non-product iff ker π has no congruence complement. And round 9-11: the "finer" structure (layers) gave (T̂, F) ≅ (νG, f) — i.e., Case 1 transport, because the layering was *derived from* f.

So the pattern: whenever the extra structure (Π) is **derivable from the original algebra (T, f)** — e.g., depth truncations — the whole enriched structure collapses to transport. The escape would require extra structure NOT derivable from (T, f).

And here's the key insight I can offer: **the depth truncation is derivable because π_n = cut_n is defined by the term structure of f.** In general, if A's projections are definable from (T,f)'s term algebra (i.e., π_i ∘ R ∈ Def(T,f)), then the enriched structure is in the "baseline" and transport. If π_i is NOT definable from (T,f) — then it's external structure on T, i.e., the same situation as rounds 12-13's "semantic anchoring".

So this round's answer converges on the SAME obstruction as round 12: the only escapes are (i) non-definable projections, or (ii) non-term-operations. And the honest status is: no new structure found; depth-Address = standard filtered/pro-algebra.

But now I should present the 3+ classes of non-truncation Address candidates, and check each:

**Class 1: Quotient/residue coordinates** — a_i(x) = x mod θ_i for congruences θ_i of T. Then A ⊆ ∏ T/θ_i. If the θ_i are definable (e.g., θ_n = depth-n congruence), this is the standard subdirect representation. If not definable, e.g., θ = "same leaf count mod m" — is that definable in (T,f)? Leaf count mod m: the homomorphism lc: T → (Z/m, +). Its kernel is definable? Hmm, "same number of leaves mod m" — is it FO-definable in (T,f)? Probably not for m ≥ 2: FO with the tree structure... Actually the two-sorted structure might make it definable with a countably infinite... Hmm, let me think. The kernel relation {(s,t) : lc(s) ≡ lc(t) mod m} — is it FO-definable in (T,f)? I believe NOT, because (T,f) is a tree and FO can't count. Indeed: the canonical example — for the free magma with one generator, consider the congruence generated by f(a,a) ~ a. Is it FO-definable? Hmm.

Actually, there's a classical fact: for a free algebra with one generator, Th(T,f) is 2ℵ0-categorical? No. Let me think about automorphism: Aut(T) = {id} for X = {a}. So everything is Aut-invariant trivially, and definability is the question. Since |Aut| = 1, invariance gives nothing; definability must be checked directly. FO-definable subsets of a free magma... Hmm, this is related to the fact that (T, f) with f injective and the term structure: FO with just f can express "is a leaf" (∃x,y f(x,y)=z), and then... For a leaf, the term is just 'a' (unique), so T has exactly one atom-less... hmm actually T has ONE atom (a). So At(x) ⟺ x = a. Now FO with f and constants. Depth is FO-definable in the sense of φ_n? We showed cut_n is definable. Leaf count mod 2 — hmm, intuitively not.

There's a relevant classical result: the free magma (or the "one-generator free algebra") — its first-order theory admits quantifier elimination? Not sure. Let me not overclaim. I'll mark this as OPEN / need to be determined, and note the direction: non-definable quotients are exactly the escape candidates from round 12's N1.

Hmm wait. Actually there's a clean and important observation: **any congruence θ on T that is FO-definable gives a quotient in the baseline. Any congruence that is NOT FO-definable gives a quotient that is arguably "new structure"** — but is it an *Address algebra*? The quotient T/θ is a homomorphic image of T, so it's a "Case 1 transport" for the *surjection* T → T/θ. Hmm. So the address map would be R = the quotient map, and ⊙ = the quotient operation = transport. So it's Case 1 again!

Hmm interesting. So quotient coordinates always give Case 1 transport (relative to the quotient algebra). So the "newness" wouldn't be in ⊙.

So what WOULD be P3? Let me think harder.

The user's target: "π_k(A⊙B) = F_k(π_k(A),π_k(B))" + "⊙ 由有限规则直接作用于 Address 内部坐标". 

Let me consider: what if A's projections are NOT homomorphisms, but ⊙ is still computed locally? E.g., A = (a_1, a_2, ...) where a_i are NOT compatible with f. Then ⊙ can't be coordinatewise. Hmm.

Or: A has more structure, e.g., A = ∏ A_i with *relations* between coordinates (not just projections). E.g., A ⊆ ∏ A_i defined by a constraint. Then ⊙ might use the constraint. But if the π_i are homomorphisms, the constraint is preserved automatically... 

I think the honest mathematical analysis is:

**Theorem (dichotomy).** Let R: T ↠ A be a surjective Σ-homomorphism (so A ≅ T/θ, θ = ker R), and let Π = {π_i} be projections on A with π_i ∘ R a homomorphism for each i. Then:
(a) If the π_i jointly separate A, then A embeds into ∏_i (T/θ_i) where θ_i = ker(π_i ∘ R), and θ = ⋂ θ_i. So A is a subdirect product of quotients of T — the standard **subdirect representation** (Birkhoff).
(b) ⊙ on A is the operation induced by f modulo θ; it is *uniquely determined* by θ, and hence by the coordinate system. Consequently, no additional structure on A can change ⊙: any two A's with the same θ are isomorphic-in-the-same-way.
(c) Therefore: "extra structure on A" can only affect ⊙ if it *fails* to be compatible with R, i.e., if it's not preserved by the quotient map — but then it's not part of the algebra A (it would break R being a homomorphism on the enriched signature).

Hmm (c) is the crux. Let me redo: the enriched structure is (A, ⊙, Π, {F_i}, coherence). Here coherence says π_i is a homomorphism. So the enriched structure = (T/θ, ⊙, subdirect presentation). And the enriched isomorphism class is determined by (θ, {θ_i}). So the "extra structure" is exactly the choice of a separating family of congruences — which is a *standard* object (subdirect representation / Birkhoff).

So P3, restricted to "Π are homomorphisms", collapses to subdirect representation = standard.

**Therefore: P3 requires Π to NOT be homomorphisms.** But then the coherence equation the user wants (π_k(A⊙B) = F_k(π_k A, π_k B)) fails! So the user's own requirement constrains Π to be homomorphisms, which forces the collapse.

Hmm! That's a sharp finding: **the user's target equation π_k(A⊙B) = F_k(π_k(A), π_k(B)) is exactly the coherence condition, and by the above, once you require it AND require the π_i to separate, the enriched structure is a subdirect representation — the standard object.** So the target equation itself is what forces the collapse.

Unless... the π_k are NOT homomorphisms but something weaker. E.g., π_k(A⊙B) = F_k(π_k A, π_k B) could hold only for a restricted class of A, B (e.g., only for A, B in the image of R). But the F_k are defined on A_k, so for A, B in the image it holds by R being a homomorphism. Hmm.

Wait — here's a potential escape: what if the π_k are homomorphisms but there are *finitely many* of them and they do NOT separate? Then R wouldn't be injective (uniqueness fails). The user's requirement 1 says "A 可唯一/足够精确表示复合概念" — uniqueness. Hmm, "唯一/足够精确" — if only "sufficiently precise", then non-separating is allowed! And then A has more points than im R, i.e., junk/structure beyond the image. And then ⊙ might use the junk.

But if π_k don't separate, R isn't injective, so distinct concepts can share addresses → violates uniqueness (round 1's core property). Unless we allow controlled collisions (round 7's non-injective route). Hmm, but the user said don't discuss controlled collision... Actually they said that in round 8/9. Let me be careful. They didn't forbid it this round, but it's a stale thread.

OK here's another angle. Let me look at the user's requirement more carefully:

"4. 不要求恢复完整 Concept" — this is important! The Address need not allow recovery of the full concept. So R need not be injective onto T... but requirement 1 says "A 可唯一/足够精确表示复合概念". Contradictory? Maybe: R is injective (unique representation) but A has extra structure beyond im R that participates. Hmm, but "不要求恢复完整 Concept" might mean: the composition ⊙ doesn't require decoding, not that information is lost.

Let me take requirement 4 as: ⊙ doesn't need to reconstruct the full concept — it just needs to be closed and correct. OK.

Let me now think about what would make a genuinely P3 example. 

Idea: **make the address space contain more structure than T, and have ⊙ use it.** E.g., A = T × L where L is a lattice/semilattice (e.g., a "resolution lattice"), and ⊙ acts on both components with L-component using L's structure. Then does π_k ∘ R being a homomorphism... 

Let's take a concrete candidate: A = the set of *finite partial trees* P (round 10's object), with ⊙ defined as "grafting" or "unification-like merge". E.g., ⊙(u,v) = the least upper bound in some order (a join). Hmm, that's the lattice route from round 4.

Let me think about a genuinely different candidate: **residue / normal form address**. E.g., A = the set of terms modulo a *non-definable* congruence (round 12's "不可定义商"). Then ⊙ = quotient operation = transport relative to the quotient, but the quotient is NOT in the baseline. So the "newness" is in the carrier, not in ⊙. Is that "Address-native computation"? The computation of ⊙ requires computing the canonical form / normal form, which may be a genuinely new computational problem (e.g., the word problem for the congruence). And that's exactly the "no-decode" question: can we compute ⊙ without decoding? For a quotient, ⊙ needs a normal form function NF, and the "decode" is NF. Hmm.

This is a real candidate: **canonical normal form address**. Example: A = terms modulo AC (associativity-commutativity), address = sorted multiset. ⊙ = multiset union + normalization. This is a classic; the address IS the normal form, and ⊙ is defined directly on addresses by a finite rule (insert into sorted list). 

Is this P3? Let's check: π = the map to the multiset; coherent; and ⊙ acts on multisets by a rule that does not decode. And the "extra structure" is the *order* on the multiset (sortedness), which is not present in T. Hmm! And note: AC-congruence is NOT FO-definable in (T,f) (I believe — since it identifies f(f(a,a),a) with f(a,f(a,a)) which have different... hmm, actually are those FO-distinguishable in (T,f)? Their structures are "same shape" — hmm, in the free magma without other structure, f(f(a,a),a) and f(a,f(a,a)) — the tree (a·a)·a vs a·(a·a). Are these FO-definable apart? They have the same "tree shape modulo mirroring"... Actually they're distinguishable: in x = f(f(a,a),a), the left child is internal; in y = f(a,f(a,a)), the left child is a leaf. "The left child is a leaf" is FO-definable. So x and y ARE distinguished by FO. But the congruence generated by AC would identify them. So the AC-congruence is not definable (it identifies FO-distinguishable elements) — hmm, careful: a congruence is definable iff its graph (a subset of T²) is definable. If θ relates points that are FO-distinguishable, that doesn't immediately make θ non-definable — the graph of θ as a subset of T² could still be definable using counting... no, FO can't count. Hmm, actually if θ is FO-definable as a relation, then... hmm, the fact that x and y are FO-distinguishable doesn't prevent θ from being definable: definability is about the relation R(x,y) on the *pair* structure. But (T,f) as a structure — can FO define "same AC-normal form"? AC-normal form requires counting/commuting, which FO on a tree can't do. I'd guess not definable, but I shouldn't claim without proof. Mark as OPEN but plausible.

OK. So now let me structure the answer:

**Part 5: Three (or four) classes of non-truncation Address:**

**Class 1: Quotient / normal-form coordinates.** A = ∏_i T/θ_i with ⊙ the quotient operations; address = the tuple of normal forms. E.g. AC-normal form, or leaf-count mod m, or "top-k skeleton". Status: standard object = **subdirect representation / homomorphic image** (Birkhoff). ⊙ is transport *relative to the quotient*. Newness can only live in the carrier (the quotient), not in ⊙. Whether the quotient is in the baseline depends on definability of θ. **Verdict: standard (subdirect product).**

**Class 2: Invariant/signature coordinates.** a_i(x) = an invariant (e.g., leaf count, height, root symbol, subtree-signature hashes). A = the set of invariant tuples, ⊙ defined by combining invariants. Status: standard = **multi-sorted algebra / product of homomorphic images**; invariants are homomorphic images, so again a quotient. **Verdict: standard.**

**Class 3: Recursive residual / automaton-state coordinates.** a_i(x) = the state reached by a tree automaton. For a bottom-up deterministic tree automaton, the run is determined by the siblings: state(x) = δ(state(l), state(r)). So A = the state set, ⊙ = δ. This is a finite algebra; T → A is a homomorphism; A is a finite quotient (recognizable). Status: standard = **recognizable congruence / finite homomorphic image**. This is the "finite tag" case from rounds 6-8! **Verdict: standard (finite quotient).**

**Class 4: Lattice / semilattice join address** (round 4's candidate). A = a semilattice (L, ∨), R(x) = the "join" of the features of x. Then ⊙ = ∨ (idempotent, commutative, associative). But wait: R(f(x,y)) = R(x) ∨ R(y) — for this to be injective, R must be injective, but then ∨ with idempotence forces R(x)∨R(x) = R(x) = R(f(x,x)) ⟹ R(x) = R(f(x,x)) ⟹ x = f(x,x), false. So **semilattice addresses cannot be injective**. That's round 5's result (injective + idempotent/associative impossible). So Class 4 fails uniqueness. **Verdict: impossible with injectivity** — a no-go.

**Class 5: Sparse / hashed coordinates with structural links.** e.g. A = hash-consed DAG (the address IS the canonical node). Then ⊙ = hash-cons(f, A, B) — computed by looking up/creating a node from the pair. Is this "intrinsic"? The address is a *pointer/canonical id*; the operation is "intern the pair". And the projection/tag is the constructor kind. Hmm — this is exactly PT-1 from round 8 (constructor tag + children). And "the address's internal structure participates": the tag is read, the children are read, a new node is formed. But that's just... the term structure itself. R = id essentially (addresses ARE the terms, canonically interned). **Verdict: standard (that's just T with interning).**

**Class 6: Resolution-indexed with intrinsic lattice of resolutions** — the "A = Q × D with state-dependent Φ" from rounds 7-9. Verdict: settled — non-product cases exist but were shown to be either isotopy-trivial (round 9-10) or operation-relabeling.

Hmm. So all classes reduce to standard objects. That's the honest finding, but I must be careful not to say "therefore all Address systems are impossible" (the user explicitly forbids collapsing "current depth-Address is standard" into "no Address can be novel"). So the answer must be III (with IV as a caveat?).

The user gives four options:
I. Found new Address-native algebraic structure.
II. No new algebraic structure, but found an Address-native computational mechanism different from indexing/memoization.
III. Current depth-Address is just standard filtered/inverse-limit representation, BUT "whether other Address-native structures exist" remains open.
IV. The original problem statement itself is not yet strict enough.

Given: depth-Address = standard (proven). Other classes: investigated 5-6, all reduce to standard objects (subdirect product, finite quotient, recognizable, interned term algebra), with one class (lattice join) impossible by injectivity. But I have NOT proven a general impossibility theorem. So the honest answer is **III**, and note that IV is a real caveat: the notion "the structure of A participates in the definition of ⊙" is a *definitional/intensional* notion that I have been formalizing extensionally (definability of ⊙ in (A, primitives)) — and the gap between intensional and extensional is exactly where a IV-type problem lives.

Hmm, actually, let me reconsider. I should give the strongest honest answer. Let me consider whether there's a real theorem I can prove:

**Theorem (Π-collapse).** Let R: T → A be injective, ⊙: A×A→A with R(f(x,y)) = R(x) ⊙ R(y). Let Π = {π_i}_{i∈I} be a family of operations on A such that π_i ∘ R is a Σ-homomorphism for all i, and Π separates... hmm, π_i range over A_i. Suppose additionally that A is generated, under ⊙ and the π_i, by... hmm.

Let me try: Suppose A's structure (operations) is exactly {⊙} ∪ {π_i}. Suppose the π_i jointly separate points of A. Then A embeds into ∏_i A_i (via the π_i), and since R is injective into A, and π_i ∘ R is a homomorphism... The subalgebra of ∏ A_i generated by R(T) is im(R) ≅ T. So A ⊇ im(R) ≅ T. If A = im(R), then A ≅ T and ⊙ = transport — P1. If A ⊋ im(R) (junk), then A has extra points; on those, ⊙ is unconstrained except for closure. Hmm! So junk gives freedom. And that's the *only* freedom. And that's what round 6-8 called "junk" (recall round 7: pairing has junk elements). Hmm.

So: **Theorem (junk dichotomy).** Under the above, either (i) A = im R (then everything is transport — P1), or (ii) A ⊋ im R, and the extra structure on A\im R is unconstrained by R and hence is *free data*, not determined by T. 

Case (ii) is "enrichment with junk". Is junk useful? In case (ii), ⊙ on junk can be defined by any rule. Any such rule is "intrinsic to A" in the sense of being definable from A's primitives, but it's not "determined by the concept algebra". Is that "Address-native computation"? Arguably yes in a trivial sense: junk is free structure. But it's exactly the "metadata" of round 8's debate. Hmm, and round 8's finding was that "not determined by T" ≠ "just metadata" — it can be a real structure (like choosing a congruence). But we also showed that the *enriched isomorphism class* is a real invariant.

OK, I think the honest and best answer:

**The dichotomy: any Address-native composition ⊙ is either (i) transport (P1) — when A = im R, or (ii) uses structure on A beyond im R (junk/enrichment) — which is genuine additional structure but is *not determined by T* and hence must be specified separately.**

And the depth-Address: A = im R (no junk! since the address space is exactly the layer sequences, which are all realized by trees... hmm, wait: the address space T̂ = νG contains infinite trees not in im(ι(T)). So there IS junk: T̂ ⊋ ι(T). Hmm! And on the junk (infinite trees), F is determined by *continuity* — and continuity is determination by T (dense subset). So the junk's operation is determined by T via the topological completion. That's why it collapses.

So: junk whose operations are determined by T (via coherence/continuity/uniqueness of extension) → collapses. Junk whose operations are NOT determined by T → genuine extra structure, but then it's extra structure on a space larger than the concept space.

This gives a clean statement:

**Theorem (Extension dichotomy).** Let R: T ↪ A injective, ⊙ realizing f, and suppose A is equipped with structure Π. Then:
(a) On im R, ⊙ is completely determined by f (＝ transport).
(b) On A \ im R, ⊙ is determined by Π only if Π is a *defining* structure for A\im R with the property that coherent extensions are unique. If the extension is unique (as in uniform completion with a dense image), then ⊙ on A is the unique coherent extension and A contributes no computational content. If the extension is *not* unique, then A carries a genuine choice — which is additional structure not determined by T.

And: the depth-Address is case (a)+unique extension (by UP, round 11). Hence collapse — PROVEN. But this does NOT exclude other A's with non-unique extension.

So Answer: **III** — current depth-Address is a standard filtered/pro-completion with unique extension (proven), and the general question of whether a non-unique-extension Address gives "new structure" remains OPEN. Hmm, but the user distinguishes II and III carefully: II = no new algebra but a new computational mechanism; III = current depth-Address standard but other Addresses open.

I'll answer III, and note that the residual open question is exactly the "non-unique extension / non-definable coordinate" route, which is where all my rounds keep landing — and which requires the "semantic anchoring" data (round 12's escape hatch), which is not constructible from (X, Σ) alone.

Hmm — that last point is actually a strong and clean conclusion: **A genuinely non-collapsing Address cannot be constructed from (X, Σ) alone; it requires data beyond the concept algebra (non-definable structure).** And that's a *theorem-shaped* statement I can support with the dichotomy argument. And it answers the user's deepest question: the original hope — "有限原子 + 有限组合规则 → Address 本身就是计算空间" — fails, precisely because everything internal to the pair (X, Σ) is already in T, and the only escape is data not derivable from (X, Σ).

That is a genuine, strong, and honest conclusion. Let me make it the centerpiece.

Now let me write the P1/P2/P3 definitions and the implications:
- P1 ⇏ P2? Yes: take R = id, ⊙ = f. Then transported trivially, but if A has no coordinates, P2 fails. Hmm, that's degenerate. Better: P1 is about the *equation*, P2 adds *coordinate-locality*. A representation can satisfy the equation without having coordinates. So P1 ⇏ P2. ✓ (trivially, by taking a coordinate-free A).
- P2 ⇏ P3? Yes — and I can prove: if Π are homomorphisms and separate, then A ≅ subdirect product of quotients of T, and ⊙ is the quotient operation = transport. So P2 ⟹ P1' (transport relative to the quotient). So coordinate-locality with homomorphic coordinates does not give P3. What P3 would need: ⊙ not determined by the coordinate data. And then the user's coherence equation π_k(A⊙B) = F_k(π_k A, π_k B) would still hold, so the coordinates are homomorphisms, so ⊙ *is* determined... unless the coordinates don't separate.

**Theorem (P3 ⟹ non-separating coordinates).** If {π_i} are homomorphisms (coherence) and jointly separate A, then ⊙ is uniquely determined by the π_i's and f: A ≅ subdirect product, ⊙ = the induced operation. Hence P3 (= ⊙ not merely coordinatewise expansion) forces either (i) the π_i do not separate (so uniqueness fails — the address loses information), or (ii) there are primitive operations/relations on A not among the π_i.

That's a clean theorem! And it's checkable. And it says: the user's target equation + uniqueness ⟹ collapse. So to escape, you must give up either coherence (π_k homomorphism) or uniqueness (separating). And giving up uniqueness = the address doesn't determine the concept — which the user's requirement 1 forbids ("A 可唯一表示").

So: **the user's own four requirements (R injective, ⊙ closed realizing f, π_k coherent, finite-local rule) are jointly satisfiable but force the collapse.** That's the sharpest possible statement and it's a theorem. Let me double check the proof.

Setup: R: T → A injective (uniqueness). ⊙: A×A→A with R(f(x,y)) = R(x)⊙R(y) (composition). π_i: A → A_i with π_i ∘ R a homomorphism and π_i(R(f(x,y))) = F_i(π_i Rx, π_i Ry) (coherence). Π separates A (needed to "observe" addresses; if not, addresses with same coordinates are indistinguishable — then A's extra points are invisible to Π but might be visible to ⊙).

Hmm, careful: I need A's structure to be "generated" by Π for the argument. Let me add: A's points are determined by Π (i.e., Π separates). Then A embeds into ∏ A_i. And ⊙ on A is an operation; is it determined by its coordinates? ⊙ is a map A×A→A. Its i-th coordinate is π_i(⊙(a,b)). If π_i ⊙ is definable from π's of a,b, then yes ⊙ is determined coordinatewise. But that's an extra assumption. Hmm.

Without that assumption, ⊙ could be "coordinatewise-invisible": e.g., ⊙(a,b) agreed with the coordinatewise rule on coordinates but differed in some non-Π-visible... no, if Π separates, then ⊙ is determined by its coordinates. So ⊙ is determined by the maps (a,b) ↦ π_i(⊙(a,b)). So the question becomes: are those maps equal to F_i(π_i a, π_i b)? 

On im R they are (coherence). Off im R, they're free! So ⊙ is determined by (i) its values on im R × im R (which is f transported) and (ii) its values elsewhere (free choice, subject to closure). And "elsewhere" = junk. So: **⊙ = transport ⊕ free junk rule.** That's exactly the dichotomy again.

So the theorem: if Π separates A and π_i ∘ R are homomorphisms, then ⊙ restricted to im R is transport, and ⊙ off im R is unconstrained. If A = im R (no junk), ⊙ = transport entirely (P1). 

And the depth-Address: A = T̂ ⊋ im(ι(T)) has junk (infinite trees), and on the junk the operation is determined by *continuity + density*, which is an additional principle tying junk to im R. So the junk isn't free — it's the unique coherent extension. Hence collapse.

So a P3 escape would need: junk whose ⊙ is NOT the unique coherent extension, i.e., **non-uniqueness of extension** — which happens when the completion/topology is such that continuity doesn't pin down values, e.g., when the image isn't dense, or when the structure isn't Hausdorff... 

That's the sharpest statement of the escape route. And it's exactly "A has structure beyond the dense image whose operations are not forced". Example: A = T ∪ {extra points} where the extra points form a set with a freely chosen ⊙ that doesn't affect the dense part. Is that interesting? It's "adding junk". The user's requirement 5 ("不是简单复制完整 Concept") is satisfied, requirement 4 ("不要求恢复完整 Concept") is satisfied. But is it *useful*? That's where computation would come in — but the user says don't make speed the criterion. Hmm, so I should present this as the open candidate and mark it.

OK, let me also handle: what does "no decode" mean strictly?

**N1 (bounded-local no-decode)**: There is a constant k such that ⊙(A,B) depends only on (π_{i_1}(A),...,π_{i_k}(A), π_{j_1}(B),...,π_{j_k}(B)) — i.e., k fixed probes into a fixed coordinate system, plus O(1) work. (This is round 13's notion.)

**N2 (structural no-decode)**: The algorithm for ⊙ only reads A's and B's *stored representation* cells and pointer links (no call to a decoder/decomposer of the full concept); i.e., the algorithm is a program over the raw representation, never invoking R^{-1} or reconstructing the term. Note N2 is *weaker* in the sense that any representation has *some* algorithm satisfying it (the transported one reads the raw data and does whatever). So N2 alone is vacuous — needs a cost or locality bound. So: N2 must be paired with a restriction (e.g., ⊙ must be in the "primitive closure" of A's primitives, and A's primitives must not include a full decode).

That's an important honest point: "no decode" cannot be defined negatively (you can always avoid naming the decode function) — it must be defined positively (the algorithm lies in a restricted class).

Good, that's a real finding worth stating.

Let me now think about the "≥3 classes of non-truncation Address" more concretely to give solid content:

1. **Quotient/normal-form addresses** (residue coordinates): a_i(x) = NF_θi(x). Standard: subdirect representation. Non-trivial only if θ non-definable.
2. **Automaton-state / recognizable coordinates**: finite quotients. Standard (finite homomorphic image). Note: these give *finite* addresses → violates uniqueness (|A| finite can't inject ℕ) unless used as extra coordinates alongside something else. Good point: any finite coordinate family is a finite quotient → the congruence is cofinite-index → for injectivity you need infinitely many coordinates, i.e., an infinite separating family. And then... that's the depth case (infinitely many coordinates).
3. **Lattice/semilattice addresses**: impossible with injectivity (round 5).
4. **Sparse/hierarchical residual addresses**: e.g., a_i(x) = "the i-th odd-level skeleton" or "level-i signature hash". Standard: filtered algebra variant.
5. **Spectral/graded addresses**: coefficients in a ring, ⊙ = convolution-like. Hmm, e.g., polynomial encoding (commutative!) — but commutative operation can't be injective (round 5). So no.
6. **Automaton + residual (like Brzozowski derivatives)**: A = set of all "residuals" of x under some structural operation. This is the classic derivative / residual construction. For trees this would be like "the set of subtrees" — a canonical invariant! Interesting: the "subtree multiset" or "subtree set" is a canonical invariant (like a Parikh vector). And ⊙ could be defined on invariants: subtree-set(x·y) = ... hmm, not closed nicely under union. But note: the "set of subtrees" of f(x,y) = {f(x,y)} ∪ sub(x) ∪ sub(y), which IS computable from sub(x), sub(y) and the addresses of x,y. Hmm, but then we need x,y themselves. So we'd need the address to include enough. Actually, the "subtree set + root" is a canonical invariant, and composition is computable — but is it injective? Probably yes (the term is recoverable from its subtree set? Hmm, maybe not — but let's say it needs exact reconstruction).

This is the classic "**canonical invariant address**". Its status: standard = "invariant of the term algebra", and it's exactly the "structure-preserving representation" case. Let me include it as a class and note that its ⊙ is computed by *set operations on invariants*, which is arguably "structure participating" — but the invariant is definable from T (it's built by structural recursion = FO-definable/term-definable), so it's in the baseline.

OK good. Now I have 5 classes to present, each with a verdict.

Let me organize the answer:

1. Where round 14 swapped the question (the trivialization).
2. Strict definitions: R, A, ⊙, and P1/P2/P3.
3. Cases 1/2/3 + the implications P1⇏P2, P2⇏P3, and P3's requirement.
4. Re-audit depth-Address: it's (A) representation homomorphism + (B) filtered algebra + (C) pro-algebra — precisely: it's the Σ-同态 into a pro-object; the "逐分辨率直接计算" is compatibility of f with the filtration = causality/compactness of f, provable in (T,f).
5. ≥3 non-truncation classes.
6. Verdict per class.
7. Status table (proved / unproved / candidate).
8. Answer III.

Let me also state the key theorem:

**Theorem (Transport–Junk Dichotomy).** Let R: T ↪ A injective, ⊙ realizing f, Π separating with π_i∘R homomorphisms. Then:
(a) ⊙|_{im R × im R} = R ∘ f ∘ (R^{-1} × R^{-1}) (transport).
(b) ⊙ is determined by (a) plus its values on (A\im R)². 
(c) If A = im R, then ⊙ is transport — P1, no intrinsic content.
(d) If A ⊋ im R and ⊙ is the *unique* coherent extension of (a) (e.g. dense image + continuity), then ⊙ is transport-plus-forced-extension — still no free content.
(e) Otherwise A carries free structure on A\im R.

And ψ: for depth-Address, A = T̂ ⊋ ι(T) but (d) applies (UP, round 11).

And the final answer: III, with the open residual being (e).

And the deepest conclusion: **everything constructible from (X, Σ) alone lands in (c) or (d); (e) requires data beyond (X, Σ)** — because any structure internal to T is captured by T's own term algebra. Hmm, is that a theorem? Let me phrase it as: any operation on A definable from (T, f) via R (i.e., π_i ∘ R ∈ Def(T,f) for all i, and ⊙ definable) lands in the baseline. So (e) requires π_i ∘ R ∉ Def(T,f) for some i — i.e., coordinates not definable in the concept algebra. That's a clean necessary condition, and it's the same "non-definable" escape as round 12.

So the final statement: **necessary condition for P3: at least one intrinsic coordinate π_i with π_i ∘ R not definable in (T, Σ).** And that's a real theorem-shaped result.

Now — the user says don't make speed the criterion, and don't conclude III from round 14's trivial baseline. So my III must be justified by the *structural* analysis (the dichotomy + the coordinate-definability necessary condition + depth-Address being case (d)), NOT by the simulation theorem. Good, that's exactly what I'm doing. And I should explicitly note that round 14's Theorem S is withdrawn as evidence (it's a trivial no-go for all representations).

Let me write it. I'll include one widget: the transport–junk dichotomy diagram.

Let me make the widget compact: a decision tree / dichotomy diagram.

Rows:
- R: T ↪ A injective, ⊙ realizing f, Π separating homomorphic
- Branch: A = im R ? → yes: P1 transport (no content)
- No (junk exists) → is ⊙ the unique coherent extension? → yes: P1+forced (no content) [depth-Address here]
- No: free structure on junk → P3 candidate ⟹ needs π_i ∘ R ∉ Def(T,Σ)

Good. Let me build the SVG.

Dimensions: 720 x ~340.

Let me lay out:
- Top box: setup (y=30-70)
- Branch question 1 (y=90-130)
- Two outcomes: left "A = im R → P1" (y=150-195), right continues
- Branch question 2 (y=150-195) on the right
- outcomes: "unique extension → P1 (depth here)" and "free junk → P3 candidate"

Hmm, let me do a vertical flow instead for clarity.

y=34: box "R: T ↪ A, ⊙ 实现 f, Π 分离且 πᵢ∘R 同态" (full width-ish)
y=88: decision "A = im R ?"
  left branch → y=140 box "P1：纯 transport" 
  right/down → continue
y=140: on the right: decision "⊙ 在 A∖im R 上是否被唯一相容延拓决定？"
  left → "P1 + 强制延拓（depth-Address）"
  right → "P3 候选：junk 上有自由结构"

Then bottom note: necessary condition for P3.

Let me write coordinates.

Actually simpler: a 3-row ladder with annotations. Let me do:

Row 1 (y 30-72): 前提框
Row 2 (y 92-172): 分叉1 左 (x 40-330): "A = im R ⟹ P1 transport"; 右 (x 390-680): "A ⊋ im R（有 junk）"
Row 3 (y 192-272): 右列再分：左 (x 390-530): "延拓唯一 ⟹ P1+（depth-Address）"; 右 (x 540-680): "延拓非唯一 ⟹ P3 候选"

That's compact. Let me write it with proper tokens.

Let me write the final response now. I'll be thorough but organized. Given the complexity, the answer will be long but that's appropriate.

Let me count the required outputs:
1. Where round 14 swapped the question
2. Strict definition of Address-native computation
3. transport/coordinate-local/intrinsic three levels
4. Re-audit depth-Address
5. ≥3 non-truncation Address classes
6. Verdict per class
7. Proved/unproved/candidate status
8. Answer I/II/III/IV

I'll match these exactly.

One more thing to verify: the claim "P2 ⟹ collapse". Let me restate precisely as the main theorem.

**Theorem (Subdirect Collapse).** Let R: T → A be injective, ⊙: A×A→A with R(f(x,y)) = R(x) ⊙ R(y). Let Π = {π_i: A → A_i}_{i∈I} with:
(i) [coherence] π_i(R(f(x,y))) = F_i(π_i R(x), π_i R(y)) for all i — i.e., π_i ∘ R is a Σ-homomorphism;
(ii) [separation] Π separates A.
Then A ≅ the subdirect product of the quotients T/θ_i (θ_i = ker(π_i ∘ R)), and ⊙ is the operation induced by f on this subdirect product; in particular ⊙ is *uniquely determined* by (T, f, {θ_i}) — no additional structure on A can alter ⊙.

Proof sketch: The map Φ: A → ∏_i A_i, a ↦ (π_i a)_i is injective (separation). Φ(A) is a subalgebra: since ⊙ and each π_i's coherence on im R... hmm, careful: Φ is an embedding iff π_i are homomorphisms for ⊙, i.e., π_i(a⊙b) = F_i(π_i a, π_i b) — but (i) only gives this for a, b ∈ im R! So off im R, π_i may not be homomorphisms, and Φ might not be an algebra embedding. Hmm. So (ii)+(i) doesn't give the collapse by itself. Need to add: (iii) π_i are homomorphisms for ⊙ (i.e., coherence holds globally, not just on im R).

The user's статement: "π_k(F(A,B)) = F_k(π_k(A), π_k(B))" — for all A, B ∈ A (globally). Yes! The user writes F: A×A→A and π_k(F(A,B)) = F_k(π_k A, π_k B) — globally. So (iii) is given by the user. 

With (iii), Φ is an algebra homomorphism, and since Φ is injective, A ↪ ∏ A_i as a subalgebra. And im(R) ⊆ A is a subalgebra isomorphic to T (since R is a homomorphism, injective). And each π_i ∘ R: T → A_i is a surjective homomorphism onto im(π_i ∘ R) ≅ T/θ_i. So Φ(A) ⊆ ∏_i (T/θ_i) and contains Φ(im R) ≅ T. So A is sandwiched: T ≅ im R ⊆ A ⊆ ∏_i T/θ_i. Hmm, so A could be a genuine intermediate algebra! That's a gap: A may contain elements not in im R but whose coordinates are values of T-quotients. So A is not necessarily a subdirect product of T-quotients... hmm, actually it is a subalgebra of ∏(T/θ_i), generated by... hmm.

So the collapse theorem as I stated needs A = im R or "A is generated by im R". Let me add: (iv) A is generated by im(R) under ⊙. Then A = im R (since ⊙ is closed on im R — indeed ⊙(Ra, Rb) = R(f(a,b)) ∈ im R). So A = im R and collapse is total. 

Without (iv), A ⊋ im R is possible with the extra elements having coordinates in ∏(T/θ_i) — that's "junk with controlled coordinates"! And on that junk ⊙ could be nontrivial and NOT forced. Interesting. Hmm, but if the coordinates are in ∏(T/θ_i) and ⊙ is coordinatewise (iii), then ⊙ on junk is determined coordinatewise by the F_i's, which are determined by f. So ⊙ on junk IS determined! Because π_i ⊙ is a homomorphism... no wait, (iii) says π_i(a⊙b) = F_i(π_i a, π_i b) — so the coordinates of a⊙b are determined by the coordinates of a and b. And by separation, a⊙b is determined by its coordinates. So ⊙ is determined by the coordinate data. And the coordinate data of junk elements are elements of ∏(T/θ_i) — fine, but ⊙ is still uniquely determined by (F_i) and the coordinate system.

Ah, so with global coherence (iii) + separation (ii), ⊙ is *uniquely determined* given the coordinates. So the collapse theorem holds: ⊙ is determined coordinatewise by the F_i, and the F_i are determined by f (on the relevant coordinates) — hmm, are they? F_i: A_i² → A_i. On im(π_i) it's determined by f. Off im(π_i)? If π_i is surjective, A_i = im(π_i) and F_i is determined. If not surjective, A_i has junk too.

OK so the theorem should be: with (ii) separation + (iii) global coherence + each π_i surjective, then ⊙ is coordinatewise determined, i.e., **P2 holds and ⊙ is the coordinatewise operation**. And it's "transport" in the sense that the coordinate operations F_i are the quotient operations of f. So the whole structure is determined by (T, f, {θ_i}).

So: **Theorem (Coordinate Determination).** Under (ii) + (iii) + surjectivity, ⊙ is uniquely determined by the coordinate family and f; no additional structure on A can change ⊙. Hence A contributes nothing beyond the coordinate system {θ_i}. And the coordinate system is a family of congruences of T — a standard object.

So P2 (with global coherence) ⟹ no intrinsic content beyond the choice of congruences. And the congruence choice is the only freedom. Which is round 8's conclusion re-derived. 

So the sharpest summary: **the only freedom in an Address algebra satisfying the user's stated requirements is the choice of a separating family of congruences of T (equivalently, the choice of a subdirect representation). Nothing else.** And that's a standard object.

And then the escape would require *dropping* global coherence or separation — i.e., either (α) π_k are not homomorphisms off im R (so ⊙ on junk is free), or (β) the π_k don't separate (so ⊙ isn't determined). Both are precisely "the address has structure beyond what its coordinates show". Case (α) is the "junk" case. 

Good. Now the depth-Address: θ_n = ker(cut_n|_T), a separating family (yes, Σ θ_n = Δ), coherence holds globally (π_n are homomorphisms), so coordinate determination applies: the structure is determined by {θ_n}. Standard: subdirect representation / pro-algebra. ✓ Matches the user's option C.

Great, this is a clean, rigorous story. Let me write it up.

For part 5, the classes: each must be checked against "is it standard?".

Let me write the answer now. I'll aim for a well-structured, rigorous response.

Also for part 7, status table:
- 已证 (theorem): coordinate determination; depth-Address = pro-algebra/subdirect/p1; lattice impossibility; the necessary condition for P3.
- 未证 (open): whether some non-definable congruence family gives genuine content; whether case (α)/(β) gives anything usable.
- 候选 (candidate): non-definable quotient addresses; junk-carrying addresses.

Let me write it.

Length: this will be long. Let me be efficient with prose and use tables.
正在请求专家建议...

已获取专家建议

Good, the advisor mostly agrees with my analysis but pushes for a nuance: it says P2 ⇏ P3 is not yet proved/refuted, and recommends checking the candidate classes. Let me think again about whether P2 ⟹ P3 can be settled.

Actually, my "coordinate determination" argument says: if the π_i are globally homomorphisms AND separate, then ⊙ is uniquely determined by the coordinate family and the F_i. And each F_i is determined by f on im(π_i). So ⊙ is "coordinatewise expansion of f" — which is exactly the user's description of P2, and the user says P3 must NOT be "just coordinatewise expansion". So under those hypotheses, P2 holds and P3 fails. So P2 ∧ separation ⟹ ¬P3.

But wait — there's the junk subtlety: A_i might contain junk (elements not in im(π_i ∘ R)). Then F_i off im(π_i) is free. And then ⊙ on A's elements whose coordinates are junk is... determined by F_i on junk, which is free but part of A's structure. Hmm. So there's freedom there. But is it "meaningful"? It's freedom in A_i's operations off the realization — i.e., again enrichment with junk.

So the sharp statement: **P2 + separation ⟹ ⊙ is determined by {F_i}; and {F_i} = {f's quotient operations} on im(π_i); off im(π_i), F_i is free.** So the freedom is exactly: (i) choice of congruence family {θ_i}; (ii) choice of F_i on junk.

And (i) is standard (subdirect representation), (ii) is junk freedom.

So P3 is impossible under (P2 + separation + no junk). If we allow junk, there's freedom, but it's "free data" — the template of rounds 8-13's debates.

Hmm. That gives a cleaner theorem than the advisor suggests. Let me state it as a dichotomy theorem and mark the honest scope.

Also, the advisor's suggestion "P1⇏P2 (give a counterexample: any non-local encoding)" — that's easy: take R: T → ℕ a Gödel coding. Then ⊙ = R∘f∘R^{-1} (transport), and there are no coordinates at all, so P2 fails. But if we insist a representation has *some* coordinates, then we need a non-local coordinate example. Better: take A = ℕ (Gödel), coordinates = the single identity coordinate. Then P2 holds trivially (one coordinate, F_1 = the transported operation). Hmm, then it's "coordinate-local" trivially. So P1 ⇏ P2 needs A to have *no* coordinate structure. Since "coordinates" is part of what a representation provides, let me define P2 as requiring the coordinate rule to be *finite-local* (bounded number of coordinates read), which is automatic if there's 1 coordinate. Hmm.

Let me reframe: P2 should be about *finitely many* coordinates with *finite* rules. A single coordinate = trivially P2. So the interesting case is when the coordinate family is infinite (like the depth-address with infinitely many π_n). Then P2 (bounded-local) becomes a real constraint: ⊙ must be computable from finitely many coordinates of each input. But depth-address's ⊙ needs F_n(u_n,v_n) for ALL n to produce the output address — so the *output* needs infinitely many coordinates, but each output coordinate depends on O(1) input coordinates. That's "coordinatewise", i.e., infinite family of local rules. So P2 in the user's sense ("有限规则直接作用于 Address 内部坐标") = an infinite family of local rules, one per coordinate. That's exactly a filtered algebra. OK.

So: P1 ⇏ P2: a transported operation with a coordinate system where the coordinate rules are NOT local. Example: R: T → ∏_n A_n where the coordinates are engineered so that output coordinate n depends on all input coordinates. Hmm, can I construct that? Take the depth-address but *permute* the coordinates: let π'_0 = π_1, π'_1 = π_0, π'_n = π_n. Then still local. To make it non-local, define coordinates that mix levels, e.g., a'_n = some hash of the whole tree truncated at level n and n+1... still local-ish. Hmm.

Actually there's a cleaner way to see P1 ⇏ P2: P1 is a *property of the equation* and P2 is a *property of the coordinate system*. A representation can satisfy the equation (trivially, by transport) while having no coordinate system at all (A = an opaque set, R a bijection, ⊙ = transport). Then P2 fails (no coordinates). And that's not degenerate — it's exactly the "opaque encoding" case the user has been worried about! So P1 ⇏ P2 is immediate and meaningful: **any bijection R: T → A gives P1 with ⊙ = transport, but if A carries no coordinates, there's nothing for a local rule to read — P2 fails.** That's the honest statement (and it's the "opaque token" case).

Hmm, but one could argue P2 with a single identity coordinate is trivial. Let me be careful and precise: define P2 as requiring a coordinate family Π and ⊙ computed by an infinite family of local rules {F_i} (each F_i reads finitely many coordinates of each argument). A single identity coordinate: Π = {id}, F = ⊙ — reads 1 coordinate each. So P2 holds. So the opaque case DOES satisfy P2 with the trivial coordinate system!

So P2 is trivial unless we require the coordinates to be *proper* (e.g., each π_i is not injective; or the family is infinite and each π_i loses information). Hmm. Let me require: P2-nontrivial = coordinate family Π with each π_i "structurally meaningful" — hmm, that's vague.

Better: the real content is not P1 vs P2 but **P2 vs P3**: does the coordinate-local realization have coordinate rules {F_i} that are *forced* by f, or is there freedom? And I showed: {F_i} is forced on im(π_i), free off it. So:

**P3 ⟺ ∃ a coordinate-local realization whose coordinate rules are not (the quotient operations of f) — equivalently, whose coordinate system is not a family of congruences of T.**

And that's a clean criterion! Because: if π_i ∘ R is a homomorphism, then θ_i = ker(π_i ∘ R) is a congruence and F_i on im(π_i) = the quotient operation. So P2 with homomorphic coordinates ⟹ the rules are the quotient operations. So P3 requires π_i ∘ R to NOT be a homomorphism — i.e., the coherence equation π_k(A⊙B) = F_k(π_k A, π_k B) to FAIL.

But the user's target equation IS the coherence equation. So: **the user's own target equation forces the collapse.** That's the sharpest result, and it's a theorem (given separation).

Wait, careful: coherence is π_k(A⊙B) = F_k(π_k(A), π_k(B)) — that says π_k is a homomorphism for ⊙ (globally, on A). Then θ_k = ker π_k is a congruence of (A, ⊙). And π_k ∘ R is a homomorphism T → A_k, so θ_k ∩ (im R)² = ker(π_k ∘ R) is a congruence of T. So the coordinate system is a family of congruences of T (restricted), and F_k = the operation on A_k ⊇ im(π_k ∘ R) ≅ T/ker(π_k∘R). So F_k on im is forced. Off im, free.

**So: P3 requires either (α) A_k has junk (F_k free off im), or (β) Π does not separate (so A has points invisible to Π).**

And (α): A_k junk = extra elements in the *coordinate algebras*. And (β): A junk. Both are "junk". So:

**Theorem (No-Junk Collapse).** If (i) R injective, (ii) ⊙ realizes f, (iii) π_k globally coherent, (iv) Π separates A, (v) every π_k is surjective onto A_k, and (vi) A_k = im(π_k ∘ R) (no junk in coordinate algebras) — then ⊙ = R ∘ f ∘ (R⁻¹ × R⁻¹) on all of A, i.e., **P1 transport, no P3.**

Hmm, do I need (v)+(vi)? If A_k has junk and a is an A-element with coordinates hitting junk, then a ⊙ b's coordinates at k are F_k(junk, ...) — free. So yes, junk allows freedom. So the theorem needs no-junk.

So the essential content: **P3 ⟺ junk (in A or in the A_k's)**. And junk is exactly "structure not determined by T". Very clean!

And then: is junk useful? Junk is free data → it's the "semantic anchoring" of round 12 → it's the only escape. And the honest statement: junk is not determined by (X, Σ), so an Address that uses junk is not constructible from (X, Σ) alone. That's the answer to part 7's deepest question.

This is a strong and clean round. Let me write it.

Let me be careful about one more: the depth-Address. A = T̂ = νG, im(ι(T)) = finite trees. So A has junk (infinite trees)! And the coordinate algebras A_k = B_k = im(π_k) — surjective, so no junk there. So (vi) holds, (iv) holds. But A has junk. Does the theorem's conclusion still hold? ⊙ on junk: for infinite trees, F((u_n),(v_n)) = (F_n(u_n,v_n))_n — this is coordinatewise, so the coordinates of a⊙b are determined by coordinates of a,b, and by separation a⊙b is determined. So even on junk, ⊙ is determined coordinatewise by the F_k's, which are forced on im(π_k) = B_k (surjective, no junk). So ⊙ IS determined on all of A. So the conclusion holds even with A-junk, PROVIDED each π_k is surjective and A_k has no junk. 

So let me restate: the theorem's real hypotheses are (i)-(iv) + "each A_k has no junk" (i.e., A_k = im(π_k ∘ R)). Then ⊙ is uniquely determined by (f, {θ_k}) — even if A has junk! Because separation + coherence + surjectivity pin down ⊙ coordinatewise.

So the theorem is stronger than I thought: **A's junk is irrelevant; what matters is the coordinate algebras' junk.** Let me verify: a ⊙ b is determined by its coordinates (separation), and coordinate k = F_k(π_k a, π_k b) (coherence), and F_k is determined by f on all of A_k (since A_k = im(π_k ∘ R), no junk, and π_k ∘ R is a homomorphism so F_k = the quotient operation). So yes: ⊙ is fully determined. ✓

So the theorem: **(i) R injective, (ii) ⊙ realizes f, (iii) global coherence, (iv) Π separates, (v') each A_k = im(π_k ∘ R) ⟹ ⊙ is uniquely determined by (T, f, {θ_k}), where θ_k = ker(π_k ∘ R).** Hence the only freedom is the choice of the separating congruence family {θ_k} — a subdirect representation. No P3.

And P3 requires violating one of (iii)-(v'). Violating (iii) = coherence fails (but that's the user's target equation). Violating (iv) = loss of uniqueness. Violating (v') = junk in coordinate algebras (free F_k).

Excellent. This is the core theorem of the round. Let me name it "Coherence Rigidity Theorem" or "坐标决定定理 — Coordinate Determination Theorem".

Now, one nuance: does (v') follow from π_k being surjective? If π_k: A → A_k is surjective then A_k = im(π_k) ⊇ im(π_k ∘ R) — could be strictly bigger if A has junk. Hmm! So (v') is not automatic. In depth-Address: π_k: T̂ → B_k is surjective, and A_k = B_k = im(π_k ∘ ι) since every B_k element is cut_k of a finite tree (round 10 proved im(π_n) characterization with the "⊥ only at depth n" and the constructibility). Yes, round 10 established im(π_n) = {u ∈ Â_n : no ⊥ at depth < n} and that these are exactly the truncations of trees in T. So A_k = im(π_k ∘ R) ✓. So depth-Address satisfies all hypotheses → ⊙ determined → no P3. ✓ 

So depth-Address: standard subdirect representation / pro-algebra. Matches user's option C.

Now let me write part 5's classes with this criterion (does the class violate (v')? i.e., does it have junk coordinate algebras?).

**Class 1: Residue/quotient coordinates** — a_k(x) = NF_{θ_k}(x) for congruences θ_k. Then A_k = T/θ_k has no junk (quotient is surjective image). So by the theorem: ⊙ determined, P1/P2, no P3. Standard (subdirect representation).
BUT: if the θ_k are not definable in (T,f), the *carrier* T/θ_k is not in the baseline. Hmm — but θ_k is still a congruence of T. The structure is still "subdirect representation of T". Standard object. So no P3. ✓ (This is important: non-definability doesn't give P3; it gives a non-baseline carrier, which is round 12's N1 territory, not P3.)

**Class 2: Invariant/signature coordinates** — a_k(x) = invariant_k(x) like leaf count, height, etc. If the invariant is a homomorphism's value (leaf count is), then again a quotient. If the invariant is NOT a homomorphism (e.g., "height" is not a homomorphism: height(f(x,y)) = 1+max(h(x),h(y)) — that's not of the form F(h(x),h(y))? Actually it IS: F(a,b) = 1+max(a,b). So height IS a homomorphism to (ℕ, F) with F(a,b)=1+max(a,b). Hmm, so yes it's a homomorphic image. In general, any structural invariant computed by bottom-up recursion IS a homomorphic image. So: **bottom-up-computable invariants = homomorphic images = quotients**. So Class 2 ⊆ Class 1.

For injectivity we'd need the family to separate — which requires infinitely many invariants (or one injective one, which brings us back to "the invariant IS the address"). If one invariant is injective, then it's a bijection T → A, ⊙ = transport, and A_k = im is everything, no junk → P1.

**Class 3: Automaton states (recognizable)** — finite quotient. Standard. Can't separate alone (finite). So must be combined.

**Class 4: Normal form / canonical factorization** — a(x) = the canonical normal form of x under some rewriting system. If the rewriting is confluent & terminating, NF exists and defines a congruence; a is the quotient map. So again Class 1. Standard.

**Class 5: Lattice/semilattice join** — impossible with injectivity (round 5: F(F(a,b),c)=F(a,F(b,c)) + injective ⟹ contradiction). So no.

**Class 6: Junk-carrying address** (the only escape): A_k has junk, so F_k off im is free. E.g., A = (a "resolution algebra" with extra states). Is that a real candidate? It's "enrichment with free data". Status: this is the ONLY escape per the theorem. And its content is *not determined by (X, Σ)*.

So the theorem gives: **P3 ⟺ junk in some coordinate algebra.** And junk cannot be derived from (X, Σ). 

That's the answer! So for part 8, the answer is III — but with a much sharper statement of what remains open: only the "junk/free structure" route remains, and it requires data beyond (X, Σ).

Hmm, but the advisor recommends III. And this reasoning supports III. Let me also acknowledge IV as a caveat about the intensional/extensional gap.

Actually — let me reconsider. Is the theorem's conclusion "P3 impossible" or "P3 impossible unless junk"? The latter. So there IS an open route (junk). So: III = "current depth-Address is standard, and whether other Address-native structures exist remains open". That's exactly III. ✓

Now let me write the widget and the answer.

Widget: the dichotomy — after the hypotheses, ⊙ is determined (P1/P2), and the ONLY escape is junk in coordinate algebras.

Let me build it as a compact flow.

Let me now write the final answer. I'll structure it per the user's 8 required outputs.

Note: I must be careful about length — this is a lot. Let me be dense but organized.

Let me draft.

Also for P1⇏P2 and P2⇏P3:
- P1 ⇏ P2: A = opaque set with bijection R, ⊙ = transport. No coordinate family → P2 fails. (But note: if we allow the trivial coordinate {id}, P2 holds. So the honest statement: P1 ⇏ P2 only if P2 requires a *proper* (non-injective) coordinate family; with the trivial family P2 is automatic. So P2 is only meaningful when coordinates are required to be proper/informative — i.e., each π_i loses information.) I'll state this precisely: define P2* = P2 with the requirement that the coordinate family is *proper* (no π_i injective) and *finite-local*. Then P1 ⇏ P2* (opaque transport has no proper coordinates, or if it does, they're not coherent).
- P2 ⇏ P3: P2 = coordinate-local with the target equation; the theorem shows this forces F_k = quotient operations, i.e., P1-like transport. So P2 ⟹ ¬P3 under no-junk. So P2 ⇏ P3 is FALSE in the no-junk case — P2 actually refutes P3! Let me state: **under hypotheses (i)-(v'), P2 ⟹ ¬P3** (the theorem). So P2⇏P3 fails; instead P2∧separating∧no-junk ⟹ ¬P3.

Hmm, the user asks "P2 ⇏ P3？" — expecting it might fail. My theorem says under good hypotheses P2 implies NOT P3. That's a stronger and more informative answer. I'll present it.

Let me write.

For the widget, let me make it a decision flow with the hypotheses box at top, then the theorem conclusion, then the escape.

OK, writing now. I'll keep the widget reasonably simple.

Actually, let me reconsider whether to render a widget at all — I've been rendering one per round. It's been useful. This round the key content is the dichotomy. Let me render a compact one.

Let me write the SVG carefully.

Layout (720 x 330):
- Box 1 (y=28-76, x=90-630): hypotheses: R 单射 + ⊙ 实现 f + πₖ 全局同态 + Π 分离 + 坐标代数无 junk
- Arrow down to Box 2 (y=96-150, x=90-630): 结论：⊙ 被 (f, {θₖ}) 唯一决定 = 子直表示 ⟹ P1/P2，无 P3
- Below: two side boxes (y=176-240): left (x=60-340): "唯一自由度：分离同余族 {θₖ}（标准对象）"; right (x=380-660): "破除任一假设 ⟹ junk：坐标代数 Fₖ 在 im 外自由"
- Bottom note (y=262-310): junk 不可由 (X,Σ) 导出 ⟹ 唯一未封闭的出口

Let me write coordinates precisely.

Box1: x=90, y=28, w=540, h=48. Two text lines.
Arrow: M360,76 V92 (marker)
Box2: x=90, y=96, w=540, h=56. Text.
Then two boxes:
Left: x=60, y=176, w=270, h=64
Right: x=390, y=176, w=270, h=64
Arrow from box2 bottom (M300,152) to left box top (L=195: M300,152 → M195,172), and (M420,152 → M525,172).
Bottom text at y=270-300.

Total H = 320.

Let me write it.

Now write the full answer. Let me organize per the 8 required output sections.

Let me be careful to mark THEOREM/PROOF/OPEN/CANDIDATE.

I'll write it now.
# 1. 第 14 轮在哪里把研究问题换掉了

**[撤回]** 第 14 轮的 Theorem S（"$\inf_{\mathrm{BIT}(S)}\mathrm{Cost}\le\mathrm{Cost}_{\mathrm{Addr}}$，故无分离，故 Address 计算坍缩为 III"）**撤回作为新颖性证据**。

**理由（你的论证，成立）**：$\mathrm{BIT}(S)$ 被定义为"允许存储任意由 $x$ 派生的数据结构"，那么对**任意**表示 $R$，$R(x)$ 本身就是一个合法的派生结构，于是

$$\forall R:\ \mathrm{Cost}_{\mathrm{BIT}(R)}\le\mathrm{Cost}_R$$

**这对所有表示一律成立**——它不是对 Address 的特殊反证，而是一个**退化为定义性不可能**的判据：若允许竞争对手复制候选表示，则"表示新颖性"在任何定义下都不可能成立（连同 $R = $ 恒等映射、$R =$ Gödel 编码、$R =$ 任意索引结构，全部被"证明"为不新）。

**Theorem S 的合法残余（窄结论，保留）**：
> Address **不能击败一个被允许复制 Address 自身的超级宽松 baseline**。

**Theorem S 不能证明的（第 14 轮的越界）**：
> Address **不具有 intrinsic computational structure**。

正确的对象是**内在判据**（$\odot$ 的**定义**是否使用 $A$ 的结构），而非**比较判据**（是否击败全部索引）。本轮全程只用内在判据。

---

# 2. Address-native computation 的严格定义

**定义 (Representation / Address structure / Native composition)**
- $R:T_\Sigma(X)\to A$ —— 表示；**要求单射**（唯一性）。
- $A=(\text{carrier};\ M_1,\dots,M_r;\ \pi_i\ (i\in I);\ R_j)$ —— Address 结构：载体、原始运算、投影/坐标/分辨率、关系。
- $\odot:A\times A\to A$ —— **native composition**，满足
$$R(f(x,y))=R(x)\odot R(y)\qquad(\forall x,y\in T)$$

**必须立即指出的关键事实**：上式**本身不是内容**。因为取

$$\odot:=R\circ f\circ(R^{-1}\times R^{-1})$$

（在 $\mathrm{im}\,R$ 上定义，再任意延拓）它是**恒真**的。故"$R$ 是 $\Sigma$-同态"这一条**零鉴别力**。内容必须来自"$\odot$ 如何被定义"。

**三层（按强度递增）**：

| 层 | 名称 | 定义 |
|---|---|---|
| **P1** | transported operation | 存在 $R,\odot$ 使 $R(f(x,y))=R(x)\odot R(y)$。**任何**单射映射都满足（取 transported $\odot$） |
| **P2** | coordinate-local | $A$ 带**proper** 坐标族 $\Pi=\{\pi_i\}$（每个 $\pi_i$ 非单射，即真的丢失信息），且 $\odot$ 由一族**局部规则** $\{F_i\}$ 给出：$\pi_i(A\odot B)=F_i(\pi_i A,\pi_i B)$，每个 $F_i$ 只读有限个坐标 |
| **P3** | intrinsic | $\odot$ **不可**由"$f$ 沿 $\{\theta_i=\ker(\pi_i\circ R)\}$ 的商运算"解释。即：存在坐标 $i$ 使 $F_i$ 在 $\mathrm{im}(\pi_i\circ R)$ **之外**有自由取值（$A_i$ 含 junk），或存在不属于任何 $\theta_i$ 的结构参与 $\odot$ |

**为何 P2 必须要求 proper 坐标**：若允许 $\Pi=\{\mathrm{id}\}$，则 $F=\odot$、$k=1$，**一切** $\odot$ 都"坐标局部"（第 13 轮已踩过此坑）。proper 性是把 P2 从空话变成真条件的唯一办法。

---

# 3. Transport / Coordinate-local / Intrinsic 三层的关系

**命题 1（P1 ⇏ P2）** [PROPOSITION]
取 $A$ = 不带任何坐标结构的集合，$R:T\to A$ 任意双射，$\odot=$ transported。则 P1 成立而 P2 失败（无 proper 坐标族）。
**这是"不透明 token"情形**——它满足用户要求的形式等式，却没有任何内部结构可供读取。**故等式本身必须被 P2 强化。**

**定理 2（坐标决定定理 · P2 实际蕴含 ¬P3）** [THEOREM]
设 (i) $R$ 单射；(ii) $\odot$ 实现 $f$；(iii) **$\pi_i$ 全局同态**：$\pi_i(A\odot B)=F_i(\pi_i A,\pi_i B)$，$\forall A,B\in A$（即用户的目标等式，**全域**成立）；(iv) $\Pi$ **分离** $A$；(v) **每个坐标代数无 junk**：$A_i=\mathrm{im}(\pi_i\circ R)$。则

$$\odot\ \text{被}\ \big(f,\{\theta_i\}_{i\in I}\big)\ \text{唯一决定},\qquad \theta_i:=\ker(\pi_i\circ R)\in\mathrm{Con}(T).$$

[PROOF] 任取 $A,B$。由 (iii)，$A\odot B$ 的第 $i$ 个坐标 $=\pi_i(A\odot B)=F_i(\pi_i A,\pi_i B)$。由 (v)，$\pi_i A,\pi_i B\in A_i=\mathrm{im}(\pi_i\circ R)$，故存在 $a,b\in T$ 使 $\pi_i A=\pi_i(Ra)$、$\pi_i B=\pi_i(Rb)$。由 (iii) 在 $Ra,Rb$ 上取值：$F_i(\pi_i Ra,\pi_i Rb)=\pi_i(Ra\odot Rb)=\pi_i(R(f(a,b)))$（用 (ii)）。故 $F_i$ 在 $\mathrm{im}(\pi_i\circ R)$ 上**等于** $f$ 的商运算 $T/\theta_i\times T/\theta_i\to T/\theta_i$。于是 $\pi_i(A\odot B)$ 完全由 $(\theta_i,f)$ 决定；由 (iv) 全部坐标决定 $A\odot B$。∎

**这个定理的三个直接后果**：

**(a) P2 ⇒ ¬P3**（在 (iii)(iv)(v) 下）。所以你问的"P2 ⇏ P3？"**答案是否定的**：在良好假设下 P2 **反驳** P3。（P2 只有在允许 junk 时才可能升到 P3，而那时它已经不是纯粹的 P2。）
**(b) 唯一的自由度是"分离同余族 $\{\theta_i\}$"** —— 即 **子直表示（subdirect representation, Birkhoff）**。这是标准对象。
**(c) P3 的精确刻画**：
$$\boxed{\text{P3}\iff\text{某个坐标代数 }A_i\text{ 含 junk}\ \ \text{或}\ \ \Pi\text{ 不分离}\ \ \text{或}\ \ \text{(iii) 全局同态律失效}}$$
在"junk 不出现"的世界里，**P3 被定理排除**——不是因为 P2 不够强，而是因为 P2 加上分离与同态律已经把 $\odot$ 钉死。

**必须强调（防止误解）**：定理 2 **不含**任何速度、成本、索引的内容。它是纯代数事实：**用户的目标等式 + 唯一性 + 分离，联合起来强制 $\odot$ 是 $f$ 沿同余族的展开。**

---

# 4. 重新审计 depth-Address

$A(x)=(\pi_0(x),\pi_1(x),\pi_2(x),\dots)$，$\pi_k(f(x,y))=F_k(\pi_k(x),\pi_k(y))$。

**逐项核对定理 2 的假设**：

| 假设 | depth-Address | 状态 |
|---|---|---|
| (i) $R=\iota:T\hookrightarrow\hat T$ 单射 | ✓ | 第 10 轮定理 E″ |
| (ii) $F_{\lim}$ 实现 $f$ | ✓ | 第 11 轮同态定理（$\Phi\circ\iota=\mathrm{id}$） |
| (iii) 全局同态 $F_n(u,v)=f(\mathrm{trunc}_{n-1}u,\mathrm{trunc}_{n-1}v)$ 定义于全部 $B_n^2$ | ✓ | 第 10 轮定理 D |
| (iv) $\{\theta_k=\ker(\mathrm{cut}_k|_T)\}$ 分离 | ✓ | 第 11 轮定理（联合单射） |
| (v) 坐标代数无 junk：$B_k=\mathrm{im}(\pi_k\circ\iota)$ | ✓ | 第 10 轮：$\mathrm{im}(\pi_n)=\{$无浅层 $\bot$ 者$\}$，且每个都可由 $T$ 中元素达到 |

**全部假设成立 ⟹ 定理 2 适用 ⟹ $F_{\lim}$ 被 $(f,\{\theta_k\})$ 唯一决定。**

**精确判定（回答第 4 部分的问题）**：

| 选项 | 判定 |
|---|---|
| A. 单纯 representation homomorphism | **是（含）**，但不够精确 |
| **B. filtered algebra** | **是（最准确的单一描述）** |
| **C. inverse system / pro-algebra** | **是（在近似/完备化层面）** |
| D. genuine intrinsic address algebra | **否**（定理 2） |
| E. 其他 | — |

**精确陈述**：depth-Address $=$ **自由 $\Sigma$-代数的一个分离同余族诱导的子直表示，其完备化为 pro-$\Sigma$-代数**。

**"逐分辨率直接计算"到底是不是"Address 内部在计算"？**
**不是。** 它是**原代数 $f$ 对过滤（filtration）的相容性**，其数学身份是：

$$\pi_k\circ f=F_k\circ(\pi_k\times\pi_k)\iff f\ \text{沿深度过滤是紧致的（causal）}$$

即"$f$ 的输出只增加 1 层深度"这一致性——**这是 $f$ 在 $(T,f)$ 中的可定义性质**（第 11 轮的 $\mathrm{cut}_n$ 公式），不是 $A$ 带来的新性质。**它属于 B（filtered algebra），不是 D。**

---

# 5. 非-truncation Address 的搜索（6 类）与逐类判定

按"$A(x)=(a_1(x),a_2(x),\dots)$，每个 $a_i$ 不必是完整 truncation"的一般形式搜索。判据：定理 2 的假设是否成立，以及 (v) 是否被破。

**类 1 — Residue / quotient coordinates**：$a_i(x)=\mathrm{NF}_{\theta_i}(x)$，$\theta_i\in\mathrm{Con}(T)$。
定理 2 直接适用（$A_i=T/\theta_i$ 是商，无 junk）⟹ $\odot$ 被 $\{\theta_i\}$ 决定，**P2，非 P3**。
**仍只是标准对象：子直表示 / 同态像。**
（注意：若某 $\theta_i$ 在 $(T,f)$ 中**不可定义**，则**载体**不在基线——但这是第 12 轮的 N1 领地，**不是 P3**。二者必须分开。）

**类 2 — Invariant / signature coordinates**：$a_i(x)=$ 某结构不变量（叶数、高度、根符号、子树签名哈希……）。
**关键观察**：底向上递归计算的不变量**就是**同态像（如 $\mathrm{height}(f(x,y))=1+\max(\mathrm{height}\,x,\mathrm{height}\,y)$ 即到 $(\mathbb N,1{+}\max)$ 的同态）。故 **类 2 ⊆ 类 1**。判为 **P2，非 P3**，标准对象。

**类 3 — Automaton-state / recognizable coordinates**：$a_i(x)=$ 自底向上树自动机到达的状态。
状态转移 $\delta(s,t)$ 使 $T\to$ 状态集成为同态 ⟹ **有限商 / recognizable 同余**。**额外发现（重要）**：有限坐标族**不可能分离无限集** $T$，故类 3 **不能单独用作 Address**（违反唯一性），只能作附加坐标。判为 **P2，非 P3**；作为**独立 Address 则不可能**。

**类 4 — Canonical normal form / factorization**：$a(x)=$ 某重写系统的规范形（若合流+终止）。
规范形映射**就是**商映射 $T\to T/{\equiv}$ ⟹ **类 1**。判为 **P2，非 P3**。

**类 5 — Lattice / semilattice join**（第 4 轮的候选）：$A$ 为半格，$\odot=\vee$。
**不可能** [THEOREM，第 5 轮重述]：$\odot$ 幂等/结合 + $R$ 单射 ⟹ $R(x)=R(f(x,x))\Rightarrow x=f(x,x)$，矛盾。**违反唯一性，排除。**

**类 6 — Junk-carrying coordinate algebra**（唯一逃逸口）：
让某 $A_i$ 含 $\mathrm{im}(\pi_i\circ R)$ 之外的元素，$F_i$ 在 junk 上自由取值。
定理 2 的 (v) 被破 ⟹ $\odot$ 获得**真正的自由** ⟹ **P3 候选**。
**关键代价**：junk 及其运算**不可由 $(X,\Sigma)$ 导出**——它必须被额外指定。故这不是"从有限原子+有限规则中涌现出的 Address 结构"，而是**外加数据**。

**汇总**：

| 类 | 定理 2 假设 | P1/P2/P3 | 是否标准对象 |
|---|---|---|---|
| 1 residue/quotient | 全满足 | P2 | ✅ 子直表示 |
| 2 invariant/signature | 全满足（⊆类1） | P2 | ✅ 同态像 |
| 3 automaton/recognizable | 全满足（仅附加） | P2 | ✅ 有限商；独立不可用 |
| 4 normal form | 全满足（＝类1） | P2 | ✅ 商代数 |
| 5 lattice join | (i) 失败 | — | ❌ 与唯一性矛盾 |
| **6 junk-carrying** | **(v) 被破** | **P3 候选** | ⚠️ 外加数据 |

---

# 6. "No decode" 的严格形式（两个强度）

**必须避免的错误定义**："存在一个叫 compose 的 API" —— 如你所指出，任何包装器都满足。

**N1 · bounded-local no-decode** [DEFINITION]
存在常数 $k$ 与**固定**索引表 $(i_1,\dots,i_k),(j_1,\dots,j_k)$，使
$$\odot(A,B)=h\big(\pi_{i_1}(A),\dots,\pi_{i_k}(A),\pi_{j_1}(B),\dots,\pi_{j_k}(B)\big)$$
且 $h$ 为固定函数；算法**不得调用** $R^{-1}$ 或任何恢复完整 Concept 的过程。
**与普通同态表示的关系**：N1 是 P2 的**算法版本**；对"每个输出坐标只读有限输入坐标"的无限坐标族，N1 取逐坐标形式（depth-Address 即此形态）。

**N2 · structural no-decode** [DEFINITION]
$\odot$ 在 $A$ 的**原始签名**（载体 + 原始运算 + 结构链接）中**可定义**（parameter-free，允许 $\Pi$ 作为签名成员），且该定义**不出现**任何"解码/展开/规范形还原"型原语。
**与普通同态表示的关系**：N2 **严格弱于** N1 的算法读法（它只要求存在定义，不管成本），但**严格强于** P1。**关键诚实点**：N2 单独**是空的**——因为任何 transported $\odot$ 都可以"避免命名解码函数"（把解码藏进定义中）。**故 N2 必须与下列任一限制合用才有内容**：(a) 定义必须只用 $A$ 的原始签名且不得引入完整项重建；(b) 加上 N1 的局部性；(c) 加上成本上界（第 13–14 轮的领域，但你已禁止把它当首要判据，故本轮只用 (a)(b)）。

**判定**：depth-Address 属于 **N1（逐坐标形式）**，且其 $h=F_n$ 是 $f$ 的截断——**算法上确实不调用解码**，但由定理 2，这不带来结构新颖性：**"不 decode"在这里之所以成立，恰恰因为每个 $F_n$ 只是 $f$ 的商运算。**

---

# 7. 状态表

**已证（THEOREM）**
1. **[坐标决定定理]** (i)–(v) ⟹ $\odot$ 被 $(f,\{\theta_i\})$ 唯一决定。**(本轮核心)**
2. depth-Address 满足 (i)–(v) ⟹ $F_{\lim}$ 是 $f$ 沿 $\{\theta_k\}$ 的子直展开，属 **P2 / filtered algebra / pro-代数**，**非 P3**。
3. 任意满足 (i)(iii)(iv) 的 Address，其唯一自由度 $=$ 分离同余族 $\{\theta_i\}$（子直表示）——**标准对象**，非 P3。
4. 类 5（半格）与唯一性矛盾。
5. **P3 的精确必要条件**：某个 $A_i$ 含 junk，或 $\Pi$ 不分离，或全局同态律失效。
6. **[撤回]** Theorem S 不是新颖性判据（对所有表示平凡成立）。

**尚未证（OPEN）**
1. **类 6（junk-carrying）是否真的给出有意义的结构**——即：junk 上的 $F_i$ 能否使 $\odot$ 具有非平凡性质（而不仅是自由数据）。
2. 是否存在一个 Address，其"结构参与 $\odot$ 的定义"在**内涵（intensional）**意义下成立，而我的**外延（可定义性）**判据看不见它。（这是本轮唯一的定义论缺口，见下。）
3. 类 1 中**不可定义**同余族给出的载体：是否可被公理化而不退化为外加数据。

**候选（CANDIDATE）**
- 类 6 junk-carrying Address：**P3 的唯一位置**，但需额外数据。

---

# 8. 最终判定

## **III。**

> **当前 depth-Address 只是标准 filtered / inverse-limit（pro-algebra、子直表示）对象——已由定理 2 与其假设核对证明。而"是否存在其他 Address-native structures"仍然开放。**

**为何是 III 而不是 I**：I 要求发现新的 Address-native 代数结构。定理 2 表明：在 (i)–(v) 下 $\odot$ 被钉死为 $f$ 的商展开；六类候选中前五类全部落入标准对象或与唯一性冲突。**没有发现 I。**

**为何是 III 而不是 II**：II 要求发现"不同于 indexing/memoization 的 Address-native 计算机制"。**本轮不讨论速度**（按你的要求），故 II 的问题在本轮**未被提问**——但即使用内在判据看，depth-Address 的 $\odot$ 是 $f$ 的商展开，属 P2，不构成新机制。

**为何是 III 而不是 IV**：
IV 是"原问题定义本身还不够严格"。但我们**已经能够精确回答**本轮的问题——$\odot$ 被 $(f,\{\theta_i\})$ 唯一决定，P3 的逃逸被定位到 junk。问题定义**足够严格到能给出定理**。IV 不成立。

**但必须如实报告一个定义论缺口（这是本轮真正的残余）**：

> 你的原始表述——"$\odot$ 的**定义直接使用** $A$ 的结构/字段"——是一个**内涵（definitional / intensional）**概念：它关切的是 $\odot$ 的**写法**。
> 而定理 2 与我的全部判据是**外延（extensional）**的：它们关切 $\odot$ 作为映射**是什么**。
> **内涵上不同的两个定义，外延上必然相同；故任何外延判据都无法区分"$\odot$ 由 $A$ 的字段算出"与"$\odot$ 由 $f$ 传输而来"。** 这个缺口不是我的疏忽，而是"结构参与计算"这一说法的**固有性质**。

**因此，把缺口补上的唯一方式不是继续找结构，而是把"参与"从内涵改为可检验的外延条件。** 定理 2 给出了它的精确形式：

$$\boxed{\text{"$A$ 的结构参与 }\odot\text{ 的定义"}\ \Longleftrightarrow\ \text{某坐标代数含 junk，或 }\Pi\text{ 不分离}}$$

**在这个外延判据下答案确定：depth-Address 不满足，故不参与——它是 filtered/pro-代数。**

---

## 回到最高优先级的问题

> **是否存在一种 $A$，使 $\odot$ 不是简单 transport / cache / index，而具有 genuinely intrinsic 的结构性质？**

**本轮的回答（不含速度）**：
- 在 (i)–(v)（$R$ 单射、$\odot$ 实现 $f$、全局同态律、$\Pi$ 分离、坐标代数无 junk）下：**不存在**。$\odot$ 被 $(f,\{\theta_i\})$ 唯一决定——定理 2。
- 在打破 (v)（坐标代数含 junk）后：**存在**自由结构，但它**不可由 $(X,\Sigma)$ 导出**——它必须被额外指定。**因此它不是"有限原子 + 有限组合规则"的涌现物。**
- 在打破 (iii)（放弃全局同态律）后：用户的目标等式 $\pi_k(A\odot B)=F_k(\pi_k A,\pi_k B)$ **本身失效**——问题不再是原来的问题。
- 在打破 (iv)（$\Pi$ 不分离）后：唯一性失效——违反你的要求 1。

**结论**：四个逃逸口，前三个要么不是原问题、要么违反唯一性；**唯一在逻辑上开放的是 junk（类 6），而它的代价是外加数据。** 这就是本轮能给出的最锋利、且诚实的定位：

> **"Address 本身成为结构化、可组合、无需 decode 的计算空间"这一目标，在 $(X,\Sigma)$ 的内部是达不到的——凡内部构造，必被定理 2 钉成 $f$ 的商展开；唯一未被定理覆盖的位置是 junk，而 junk 正是 $(X,\Sigma)$ 之外的数据
